import styled from 'styled-components';

const Styled = {
  TranslatorContainer: styled.span`
    display: block;
    position: absolute;
    right: 16px;
    top: 16px;
  `,
  Select: styled.select`
    border: none;
    background: none;
    margin-left: 8px;
    color: #fff;
  `,
};

export default Styled;
